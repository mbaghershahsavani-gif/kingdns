# KingDNS DNS Engine v3.2

Authenticated Controller Communication Layer.

Added:

- controller authentication foundation
- token provider
- authenticated node discovery preparation
- heartbeat authentication structure
- secure routing preparation

Next:

- real JWT validation
- live controller HTTP calls
- PostgreSQL routing rules
- Redis cache
- multi-node DNS routing
