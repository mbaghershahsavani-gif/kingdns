# KingDNS DNS Engine v1.2

Production DNS server preparation.

Added:

- resolver request models
- response models
- routing engine foundation
- TTL cache validation
- agent integration layer
- controller routing layer

Next:
- real DNS protocol implementation
- UDP/TCP listeners
- full routing decisions
- production resolver tests
