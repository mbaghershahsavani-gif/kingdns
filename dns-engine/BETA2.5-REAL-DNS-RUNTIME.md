# KingDNS DNS Engine v2.5

Real DNS runtime phase.

Fixed:

- server startup wiring
- UDP DNS listener startup
- TCP DNS listener startup
- runtime handler foundation
- local DNS validation path

Next:

- connect database records
- generate real DNS answers
- activate Redis cache
- connect controller routing
