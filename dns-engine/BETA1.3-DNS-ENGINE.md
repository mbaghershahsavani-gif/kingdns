# KingDNS DNS Engine v1.3

Real DNS server preparation.

Added:

- UDP listener foundation
- TCP listener foundation
- query processor
- latency routing selection
- memory cache
- query logging pipeline
- DNS configuration

Next:

- implement DNS packet protocol
- integrate miekg/dns
- production resolver tests
